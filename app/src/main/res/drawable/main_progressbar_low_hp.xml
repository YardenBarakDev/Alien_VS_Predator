<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/main_SB_hpLost"
                android:centerColor="@color/main_SB_hpLost"
                android:endColor="@color/main_SB_hpLost"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#A80303"
                    android:centerColor="#B30000"
                    android:endColor="#B60202"
                    />
            </shape>
        </clip>
    </item>
</layer-list>