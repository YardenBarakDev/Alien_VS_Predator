<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/game_PB_hpCritical"
                android:centerColor="@color/game_PB_hpCritical"
                android:endColor="@color/game_PB_hpCritical"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/game_PB_hpLost"
                    android:centerColor="@color/game_PB_hpLost"
                    android:endColor="@color/game_PB_hpLost"
                    />
            </shape>
        </clip>
    </item>
</layer-list>